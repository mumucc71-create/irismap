(function(){const form=document.querySelector("#reservationForm"),status=document.querySelector("#reservationStatus");const session=String(localStorage.getItem("irisMappingSession")||"").replace(/\D/g,"");if(session)form.elements.phone.value=session;form.addEventListener("submit",event=>{event.preventDefault();const data=Object.fromEntries(new FormData(form));const key=`irisReservationDraft:${String(data.phone).replace(/\D/g,"")}`;localStorage.setItem(key,JSON.stringify({...data,requestedAt:new Date().toISOString(),status:"확인 대기"}));status.textContent="예약 요청을 이 브라우저에 저장했습니다. 실제 확정 안내가 필요합니다."})})();
